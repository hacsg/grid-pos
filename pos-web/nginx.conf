server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    add_header Cache-Control "public, max-age=3600";

    location / {
        try_files $uri $uri/ /index.html;
    }
}
